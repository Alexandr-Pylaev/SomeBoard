services:
  backend:
    image: backend
    build:
      context: .
      dockerfile: Backend/Dockerfile

  frontend:
    image: frontend
    build:
      context: .
      dockerfile: Frontend/Dockerfile

